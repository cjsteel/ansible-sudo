---
# sudo_defaults:
#  - defaults: "# %wheel	ALL=(ALL) NOPASSWD: ALL"
#  - name: user1
#    defaults: requiretty
# sudo_users:
#  - name: '%group1'
#  - name: 'bar'
#    nopasswd: yes
#  - name: '%group2'
#    commands: '/bin/ls'
#

# package name (version)
sudo_package: sudo

# list of username or %groupname
sudo_users: []

# list of username or %groupname and their defaults
sudo_defaults: 
  - name: ansible
    defaults: "# %wheel	ALL=(ALL) NOPASSWD: ALL"

